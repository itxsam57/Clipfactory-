from __future__ import annotations

def upload(*args, **kwargs):
    raise RuntimeError(
        "TikTok autopost is disabled by default. Enable only after your TikTok "
        "developer app has approved Content Posting API access for a compliant use case."
    )
